spring:
  profiles:
    active:
      default
          
eureka:
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone : http://discovery:8761/eureka/
  instance:
    prefer-ip-address: false
    hostname: localhost